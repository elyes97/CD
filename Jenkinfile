pipeline {
  environment {
    registry = "elyes97/cd"
    registryCredential = 'dockerhub12'
    dockerImage = ''
  }
 agent any
  stages {
    stage('Cloning Git') {
      steps {
        git 'https://github.com/elyes97/CD.git'
      }
    }
            stage('Build') {
            steps {
	    sh label: '', script: 'mvn install'
                echo 'Building..'
            }
        }
    stage('Building image') {
      steps{
        script {
          dockerImage = docker.build registry + ":$BUILD_NUMBER"
        }
      }
    }
    stage('Deploy Image In dockerhub') {
  steps{
    script {
      docker.withRegistry( '', registryCredential ) {
        dockerImage.push()
      }
    }
  }
}
  }
}


        
